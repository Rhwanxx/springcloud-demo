spring:
  cloud:
    config:
      #uri: http://localhost:8081/
      name: wanrh-config
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: spring-cloud-config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8800/eureka/